#include <cstdio>
#include <cstring>
#include <algorithm>
#include <cmath>
#include <vector>

using namespace std;

int a[2000] = {6,28,496,8128};

int main() {
    int n;scanf("%d",&n);
    for(int i = 1;i <= n;i++) {
        int x;scanf("%d",&x);
        if(x <= 5) {
            printf("-1\n");
        }
        else {
                    break;
                }
            }
        }
    }
    return 0;
}